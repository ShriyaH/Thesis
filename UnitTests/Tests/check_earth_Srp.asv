A = 0.2;
c = 2.999*10^8;
W = 1360;
r = 1.8;
a = 500e3+6378e3;
mu = 3.986004418e14;
    
f = (r*W*A)/c;
T = 2*pi*sqrt(a^3*mu);
n = sqrt(mu/a^3);
alpha = 70;

a_max = (f/n^2)*cosd(alpha);
i_max = (f/(n^2*a))*sind(alpha);
o_max = 2*(f/(n^2*a))*sind(alpha);